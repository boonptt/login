var fullName = "Nguyen Van A";
var age = 20;
var job = "Student";

alert(`${fullName} is ${age} years old and works as a ${job}`);
console.log(`${fullName} is ${age} years old and works as a ${job}`);

console.error("Error message");
console.warn("Warning message");

confirm("Are you sure?");
